function setup() {
  createCanvas(600, 400);

  noStroke();
}

function draw() {
  background(0);

  let a=10;

  fill(255,255,255,5);
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

    a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

    a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

    a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);

  a+=10;
  ellipse(width/2,height/2,a,a);




}
